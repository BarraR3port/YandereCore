package com.podcrash.comissions.yandere.core.velocity.user;

import com.podcrash.comissions.yandere.core.common.data.user.User;

import java.util.UUID;

public final class VelocityUser extends User {
    
    public VelocityUser(String name, UUID uuid){
        super(name, uuid);
    }
}
